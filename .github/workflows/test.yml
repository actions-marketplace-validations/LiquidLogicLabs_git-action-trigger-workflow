name: Test

on:
  workflow_call:  # Called by CI workflow
  workflow_dispatch:  # Allow manual triggering

concurrency:
  # Explicit prefix (not ${{ github.workflow }}) so this reusable workflow does not
  # share a concurrency group with the caller (ci.yml) — caller/callee on the same
  # group with cancel-in-progress: true triggers GitHub's deadlock detection.
  group: Test-${{ github.event.pull_request.number || github.ref || github.sha }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  test:
    # Always run - this workflow only has workflow_call and workflow_dispatch triggers
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          # No step in this job pushes; the credential is not needed.
          persist-credentials: false

      - name: Setup Node.js
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: '24'
          # setup-node enables package-manager caching by default; a cache
          # restored from a PR run must not reach a publishing job. Removing
          # `cache: 'npm'` alone is NOT enough -- the default still caches.
          package-manager-cache: false

      - name: Install dependencies
        run: npm ci

      - name: Verify Jest is installed
        run: npx jest --version

      - name: Build
        run: npm run build

      - name: Run tests
        run: npm run test:unit
      - name: Coverage thresholds
        # Runs the whole suite once with coverage and fails if it drops below the
        # ratchet in jest.config. The unit and integration steps above each cover only
        # part of the tree, so neither alone is a meaningful coverage measurement --
        # that is why this is a separate step rather than a flag on one of them.
        # e2e suites are excluded: they need real forge tokens, which live in the TEST
        # environment used by e2e-tests.yml, not in this job. Including them made this
        # step fail on 11 unrelated e2e errors rather than on coverage.
        run: npx jest --coverage --testPathIgnorePatterns='/e2e/'

